import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromRGBO(104, 164, 244, 1);
  static const primaryColorLight = Color.fromRGBO(239, 245, 252, 1);
  static const backgroundColor = Colors.white;
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;
  static const redColor = Colors.red;
  static const blueColor = Colors.blue;
  static var lightGrayColor = Colors.grey[300];

// *********************** BUTTON COLORS ******************************************************************
  static const primaryButtonColor = Color(0xFF5F95DC);
  static const secondaryButtonColor = Color(0xFFFFFFFF);

// *********************** SPECIALTY AND SERVICES LABLE BTN COLORS ****************************************
  static const primaryLableColor = Color.fromRGBO(239, 245, 252, 1);
  static const secondarylableColor = Color(0xFFFFFFFF);

// *********************** CARD COLORS ********************************************************************
  static const primaryCardColor = Color(0xFFFFFFFF);

// *********************** TEXT COLORS ********************************************************************
  static const primaryTextColor = Color(0xFF4A4E5A);
  static const secondaryTextColor = Color(0xFF4A4E5A);
  static const secondaryLightTextColor = Color(0xFF6B7280);
  static const textWhiteColor = Color(0XFFFFFFFF);
  static const hintTextColor = Color.fromRGBO(119, 130, 147, 1);

// *********************** ICON COLORS *********************************************************************
  static const ratingStarColor = Color(0xFFFF9620);
  static const primaryIconColor = Color.fromRGBO(119, 130, 147, 1);
  static const logoutIconColor = Colors.red;
// *********************** HOSPITAL OPEN AND CLOSE BTN COLORS **********************************************
  static const isOpenBtnColor = Color(0xD6D2F5E0);
  static const isOpenTextColor = Color(0xFF1FCD64);
  static const isClosedBtmColor = Color(0xFFFDEBEC);
  static const isClosedTextColor = Color(0xFFF1616D);

// *********************** ERROR AND SUCCESS COLORS ********************************************************
  static const Color successColor = Color(0xFF1FCD64);
  static const Color failureColor = Color(0xFFF1616D);

// *********************** INPUT FIELD COLORS **************************************************************
  static const primaryInputfieldBgColor = Color(0xFFEFF5FC);

// *********************** SHIMMER LOADING STATE COLORS ****************************************************
  static const shimmerBaseColor = Color.fromARGB(255, 230, 230, 235);
  static const shimmerHighlightColor = Color.fromARGB(122, 230, 230, 235);

// *********************** SLIDER COLORS *******************************************************************
  static const activeSliderColor = Color.fromRGBO(107, 114, 128, 1);
  static const inActiveSliderColor = Color.fromRGBO(224, 224, 224, 1);

// *********************** CHAT BUBBLE COLORS **************************************************************
  static Color? chatBotChatBubbleColor = Colors.grey[100];
  static Color? userChatBubbleColor = const Color(0xFF5F95DC);

// *********************** AVATAR COLORS *******************************************************************
  static const avatarBorderColor = Color.fromRGBO(230, 230, 230, 1);
  static const avatarBackgroundColor = Color.fromRGBO(248, 248, 250, 1);
  


// *********************** BORDER COLORS *******************************************************************
  static const buttonBorderColor =  Color.fromARGB(121, 168, 168, 173);

}
