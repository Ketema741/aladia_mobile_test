import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.black;
  static const primaryColorLight = Color.fromARGB(255, 28, 28, 28);
  static const backgroundColor = Colors.black;
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;
  static const redColor = Colors.red;
  static const blueColor = Colors.blue;
  static var lightGrayColor = Colors.grey[300];

// *********************** BUTTON COLORS ******************************************************************
  static const primaryButtonColor = Colors.black;
  static const secondaryButtonColor = Color(0xFFFFFFFF);

// *********************** SPECIALTY AND SERVICES LABLE BTN COLORS ****************************************
  static const primaryLableColor = Color.fromRGBO(239, 245, 252, 1);
  static const secondarylableColor = Color(0xFFFFFFFF);

// *********************** CARD COLORS ********************************************************************
  static const primaryCardColor = Color(0xFFFFFFFF);

// *********************** TEXT COLORS ********************************************************************
  static const primaryTextColor = Color(0xFF4A4E5A);
  static const secondaryTextColor = Color(0xFF4A4E5A);
  static const secondaryLightTextColor = Color(0xFF6B7280);
  static const textWhiteColor = Color(0XFFFFFFFF);
  static const hintTextColor = Color.fromRGBO(119, 130, 147, 1);


// *********************** ERROR AND SUCCESS COLORS ********************************************************
  static const Color successColor = Color(0xFF1FCD64);
  static const Color failureColor = Color(0xFFF1616D);

// *********************** INPUT FIELD COLORS **************************************************************
  static const primaryInputfieldBgColor = Color(0xFFEFF5FC);

// *********************** SLIDER COLORS *******************************************************************
  static const activeSliderColor = Color.fromRGBO(107, 114, 128, 1);
  static const inActiveSliderColor = Color.fromRGBO(224, 224, 224, 1);

// *********************** BORDER COLORS *******************************************************************
  static const buttonBorderColor =  Color.fromARGB(121, 168, 168, 173);

}
